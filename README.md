# ElectrocardiogramArduino
Plotting multiple sensor readings in realtime using Matplotlib and PySerial.

Computer communicates via serial port to Arduino microcontroller connected to a pulse sensor.

